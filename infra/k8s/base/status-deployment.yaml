apiVersion: apps/v1
kind: Deployment
metadata:
  name: status
  namespace: flowmaestro
  labels:
    app: flowmaestro
    component: status
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flowmaestro
      component: status
  template:
    metadata:
      labels:
        app: flowmaestro
        component: status
    spec:
      serviceAccountName: flowmaestro-sa
      containers:
        - name: status
          image: us-central1-docker.pkg.dev/flowmaestro-prod/flowmaestro/status:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          resources:
            requests:
              cpu: 25m
              memory: 32Mi
            limits:
              cpu: 100m
              memory: 64Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            successThreshold: 1
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 2
            successThreshold: 1
            failureThreshold: 3
          securityContext:
            runAsNonRoot: true
            runAsUser: 101  # nginx user
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop:
                - ALL
      restartPolicy: Always

---
apiVersion: v1
kind: Service
metadata:
  name: status-service
  namespace: flowmaestro
  labels:
    app: flowmaestro
    component: status
spec:
  type: ClusterIP
  selector:
    app: flowmaestro
    component: status
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  sessionAffinity: None

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: status-hpa
  namespace: flowmaestro
  labels:
    app: flowmaestro
    component: status
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: status
  minReplicas: 1
  maxReplicas: 2
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
        - type: Percent
          value: 50
          periodSeconds: 60
    scaleUp:
      stabilizationWindowSeconds: 0
      policies:
        - type: Percent
          value: 100
          periodSeconds: 30
        - type: Pods
          value: 1
          periodSeconds: 30
      selectPolicy: Max
