apiVersion: apps/v1
kind: Deployment
metadata:
  name: static
  namespace: flowmaestro
  labels:
    app: flowmaestro
    component: static
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flowmaestro
      component: static
  template:
    metadata:
      labels:
        app: flowmaestro
        component: static
    spec:
      serviceAccountName: flowmaestro-sa
      containers:
        - name: static
          image: us-central1-docker.pkg.dev/flowmaestro-prod/flowmaestro/static:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 2
            successThreshold: 1
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 3
            periodSeconds: 5
            timeoutSeconds: 2
            successThreshold: 1
            failureThreshold: 3
          securityContext:
            runAsNonRoot: true
            runAsUser: 101  # nginx user
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop:
                - ALL
      restartPolicy: Always

---
apiVersion: v1
kind: Service
metadata:
  name: static-service
  namespace: flowmaestro
  labels:
    app: flowmaestro
    component: static
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
spec:
  type: ClusterIP
  selector:
    app: flowmaestro
    component: static
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  sessionAffinity: None

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: static-hpa
  namespace: flowmaestro
  labels:
    app: flowmaestro
    component: static
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: static
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
        - type: Percent
          value: 50
          periodSeconds: 60
    scaleUp:
      stabilizationWindowSeconds: 0
      policies:
        - type: Percent
          value: 100
          periodSeconds: 30
