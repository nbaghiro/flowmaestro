apiVersion: v1
kind: ConfigMap
metadata:
  name: gcs-config
  namespace: flowmaestro-staging
data:
  # Staging GCS bucket for knowledge documents
  # Update this with the actual staging bucket name from Pulumi
  GCS_BUCKET_NAME: "flowmaestro-knowledge-docs-flowmaestro-staging"
